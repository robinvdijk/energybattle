<%= form_for TeamRelation.new do |f| %>
	<%= f.hidden_field :battle_id, value: @battle.id %>
	<%= f.hidden_field :user_id, value: @current_user.id %>
	<%= f.hidden_field :team, value: team %>
	<%= f.submit "Join", class: "join_button" %>
<% end %>