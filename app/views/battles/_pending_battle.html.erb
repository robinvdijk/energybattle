<h1>Pending Battle</h1>
<div class="container">
	<div class="quarter_of_container">
		<h1>Instellingen</h1>
		<p>Thema: <%= @battle.theme%></p>
	</div>
	<div class="quarter_of_container">
		<h1>Team 1</h1>
		<div class="pusher">
			<% @host_team_members.each do |team|%>
				<div class="member">
					<%= team.user.first_name %>
					<%= team.user.email %>
					<% unless team.user.id != @current_user.id %>
						<%= link_to "Leave", team, method: :delete, data: { confirm: "Wil je echt deze battle verlaten?"} %>
						<%= link_to "Switch teams", switch_team_relation_path(team), method: :patch %>
					<% end %>
				</div>
			<% end %>
		</div>
		<% if user_in_one_team %>
			<div class="join_button"><%= render "team_relation/host_team_form" %></div>
		<% end %>
	</div>

	<div class="quarter_of_container">
		<h1>Team 2</h1>
		<div class="pusher">
			<% @opponent_team_members.each do |team|%>
				<div class="member">
					<%= team.user.first_name %>
					<%= team.user.email %>
					<% unless team.user.id != @current_user.id %>
						<%= link_to "Leave", team, method: :delete, data: { confirm: "Wil je echt deze battle verlaten?"} %>
						<%= link_to "Switch teams", switch_team_relation_path(team), method: :patch %>
					<% end %>
				</div>
			<% end %>
		</div>
		<% if user_in_one_team %>
			<div class="join_button"><%= render "team_relation/opponent_team_form" %></div>
		<% end %>
	</div>

	<div class="quarter_of_container">
		<h1>Uitnodigen</h1>
		<%= render 'users/search_members' %>
	</div>
	<%= button_to "Prepare Battle", prepare_battle_path(@battle), method: :patch if @battle.host_id == @current_user.id %>
</div>